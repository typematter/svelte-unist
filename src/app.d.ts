// See https://kit.svelte.dev/docs/types#app
// for information about these interfaces
declare global {
	// Global function to render math in an element
	function renderMathInElement(element: HTMLElement): void;

	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
